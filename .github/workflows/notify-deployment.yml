name: Deployment Notifications

on:
  workflow_run:
    workflows: ["Production Deployment", "Test Deployment to dd_szt15b"]
    types: [completed]

jobs:
  notify:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion != 'skipped' }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Notify on success
      if: ${{ github.event.workflow_run.conclusion == 'success' }}
      run: |
        echo "✅ Deployment completed successfully!"
        echo "Workflow: ${{ github.event.workflow_run.name }}"
        echo "Commit: ${{ github.event.workflow_run.head_sha }}"
        echo "Branch: ${{ github.event.workflow_run.head_branch }}"
        
        # Add notification logic here (Slack, Teams, etc.)
        # Example Slack notification:
        # curl -X POST -H 'Content-type: application/json' \
        #   --data '{"text":"✅ GitOps Deployment Successful: ${{ github.event.workflow_run.name }}"}' \
        #   ${{ secrets.SLACK_WEBHOOK_URL }}
    
    - name: Notify on failure
      if: ${{ github.event.workflow_run.conclusion == 'failure' }}
      run: |
        echo "❌ Deployment failed!"
        echo "Workflow: ${{ github.event.workflow_run.name }}"
        echo "Commit: ${{ github.event.workflow_run.head_sha }}"
        echo "Branch: ${{ github.event.workflow_run.head_branch }}"
        
        # Add notification logic here (Slack, Teams, etc.)
        # Example Slack notification:
        # curl -X POST -H 'Content-type: application/json' \
        #   --data '{"text":"❌ GitOps Deployment Failed: ${{ github.event.workflow_run.name }}"}' \
        #   ${{ secrets.SLACK_WEBHOOK_URL }}
